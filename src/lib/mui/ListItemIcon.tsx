import { default as MuiListItemIcon } from '@mui/material/ListItemIcon';
import { styled } from '@mui/material/styles';

export const ListItemIcon = styled(MuiListItemIcon)({
    minWidth: '2.25em !important',
});