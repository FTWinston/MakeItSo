import { ShipSystem } from 'src/types/ShipSystem';

interface Props {
    system: ShipSystem;
}

export const SystemRepair: React.FC<Props> = (props) => {
    return (
        <div>System log!</div>
    );
};