import { StoryObj } from '@storybook/react';
import { TargetSelectionItem } from './TargetSelectionItem';

export default {
  title: 'Sensors/Target Selection/Item',
  component: TargetSelectionItem,
};

type Story = StoryObj<typeof TargetSelectionItem>;

export const Basic: Story = {
  args: {
    
  },
};
